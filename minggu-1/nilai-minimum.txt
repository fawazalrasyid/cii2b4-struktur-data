function minimum(T: tabInt; N: integer) -> integer
kamus
    min : integer
    k : integer
    idx : integer
alghoritma
    min = T[0]
    idx = 0
    for k = 1 to N-1 do
        if T[k] < min then
            min = T[k]
            idx = k
        end if
    end for
    return idx
endfunction

function minimum 