
void createStack(stack &S) {
    S.top = NULL;
}

bool isEmpty(stack S) {
    return (S.top == NULL);
}

bool isFull(stack S) {
    return false;
}

void push(stack &S, int x) {
    node *p = new node;
    p->data = x;
    p->next = S.top;
    S.top = p;
}

for (int i = 0; i < n; i++) {
    int x;
    cin >> x;
    push(S, x);
}

void printInfo(stack S) {
    for (i = 0; i < n; i++) {
        cout << S.top->data << " ";
        S.top = S.top->next;
    }
}

struct stack {
    infotype 
}